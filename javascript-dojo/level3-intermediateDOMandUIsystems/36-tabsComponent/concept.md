# Concepts Learned

* Dynamic rendering using `innerHTML`
* Rendering UI from an array of objects
* Using `onclick` with function parameters
* Managing active state using booleans
* Single active selection pattern (tabs)
* Re-rendering UI after state changes
